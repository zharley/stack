#! /bin/bash
set -e

MY_BRANCH=`git rev-parse --abbrev-ref HEAD`

echo "=> Current branch is '$MY_BRANCH'. Will attempt to merge in master."

git checkout master
git pull

git checkout $MY_BRANCH
git merge master
